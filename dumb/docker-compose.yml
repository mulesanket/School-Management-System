version: "3.8"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: schoolspider
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: devpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

